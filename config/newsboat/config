auto-reload yes
reload-time 30
reload-threads 4
max-items 25

cache-file "/home/william/.config/newsboat/cache.db"

# use notify-send to send new item notifications
notify-program "notify-send"

# use firefox to browse
browser "firefox -new-tab %u"

# use mpv to open with ,-y
macro y set browser "mpv %u"; open-in-browser-and-mark-read ; set browser "firefox -new-tab %u"
